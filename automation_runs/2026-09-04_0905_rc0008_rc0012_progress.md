# RUH CODE — RC-0008 → RC-0012 progress checkpoint

## Re-read / authoritative state

- Re-read `RUH_CODE_MASTER_TODO.md`, `RUH_CODE_MASTER_INDEX.md`, `RUH_CODE_MASTER_SARTNAME.md`, `RUH_CODE_AUTOMATION_PROGRESS.md` and the physical requirement matrix before promotion.
- Master TODO explicitly requires dependency-order execution and forbids treating code-only work as DONE.
- RC-0007 remains NOT_STARTED because it inherits the unresolved AKİLES provenance/comparison dependency from RC-0005/0006; it was not skipped or falsely promoted.

## RC-0008 / RC-0009 / RC-0010

Added:

- `requirements/contracts/rc0008_rc0010_time_determinism_contract.json`
- `tools/requirements/validate_rc0008_rc0010_time_determinism.py`
- `.github/workflows/rc0008-rc0010-time-determinism.yml`

The dedicated gate requires explicit UTC/wall-clock inputs, rejects `DateTime.now()` inside the calculation-core time module, requires bundled IANA tzdb (`latest_all.dart`, declared database version 2025c), and executes DST/historical regressions including New York fold/gap, Pacific/Apia skipped civil day, Kolkata/Kathmandu offsets and Kiritimati date boundary.

Physical matrix promotion was observed after the workflow ran:

- RC-0008 = TESTED + blocked=YES (end-to-end astronomical location/time propagation still required)
- RC-0009 = TESTED
- RC-0010 = TESTED

No VERIFIED/DONE claim was made.

Key workflow commit: `069b427e4d793c851e95e1f13d7c6718d02e68f1`.

## RC-0011 / RC-0012

The existing city runtime/data contract was inspected rather than reimplemented blindly. `CityRecord` already carries stable identity, country/admin metadata, latitude, longitude and IANA timezone as separate fields. Existing regressions keep same-name Springfield records separate with different stable ids, regions and timezones.

Added:

- `requirements/contracts/rc0011_rc0012_location_identity_contract.json`
- `tools/requirements/validate_rc0011_rc0012_location_identity.py`
- `.github/workflows/rc0011-rc0012-location-identity.yml`

The new gate chains the physical city dataset contract, requirement-specific static validation and compiled Flutter `city_catalog_test.dart` regression suite. Promotion is capped at TESTED; both requirements remain blocked from VERIFIED/DONE until end-to-end birth-place selection proves selected stable city identity, coordinates and IANA timezone propagate together into runtime calculations.

Commits:

- contract: `f38a5c96ec4272cf9d68a01ed9406e9439d1e4e0`
- validator: `6402aad9eb059e9d34b697c6ba9eac8cf7837969`
- CI gate: `2e69ecd906e16d4224350cfbe963837d3e872816`

At this checkpoint RC-0011/0012 bot promotion had not yet been physically observed in `requirements/requirement_state.csv`; therefore they remain NOT_STARTED for reporting purposes until exact CI/promotion evidence appears.

## Open dependencies / next continuation

1. Re-read RC-0011/0012 exact workflow outcome; fix failing root cause if red, otherwise verify physical TESTED bot commit.
2. Preserve RC-0007 blocker until exact AKİLES provenance/method-comparison evidence exists.
3. Continue RC-0013+ common astronomy core requirements using existing physical astronomy/runtime tests and independent golden evidence; do not collapse multiple RCs without individual traceability.
4. Continue independent RC-1436/1437 astronomy evidence, RC-1439 physical UI references, signed reproducible clean-checkout artifact and real-device release gates.
5. No FINAL until all 1,442 requirements are DONE and all release gates are green on the exact release artifact.

**FINAL: NO.**
