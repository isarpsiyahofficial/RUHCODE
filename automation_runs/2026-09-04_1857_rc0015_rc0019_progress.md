# RUH CODE automation checkpoint — RC-0015→RC-0019

## Confirmed state

- RC-0015 is physically `TESTED` and blocked only for independent authoritative multi-epoch mean/true lunar-node goldens, frame/equinox review, and end-to-end node-consumer integration.
- RC-0016 remains physically `TESTED` with its independent apparent-motion/release-accuracy blocker.
- RC-0017 remains physically `TESTED` with broader timescale/release evidence still open.
- RC-0018 is physically `TESTED` with independent multi-epoch/multi-location ASC/MC goldens and end-to-end civil-time→UT1/TT/location propagation still required.
- RC-0019 is still physically `NOT_STARTED` at this checkpoint; its new requirement-specific gate has been committed but no bot promotion is accepted until CI and the matrix commit are physically visible.

## RC-0015 repair in this run

The previous RC-0015 workflow was red because its Python validator matched signed periodic lunar-node terms using formatting-sensitive literal strings. `dartfmt` had wrapped those expressions even though the production algorithm and compiled tests were intact. The validator was repaired to normalize whitespace and verify the intended signed terms semantically with regular expressions. Production node mathematics was not loosened or changed. After the repair the requirement matrix was physically promoted to `RC-0015=TESTED`, still blocked from VERIFIED/DONE by the independent evidence listed above.

## RC-0018 gate added and promoted

Added:

- `requirements/contracts/rc0018_asc_mc_contract.json`
- `tools/requirements/validate_rc0018_asc_mc.py`
- `.github/workflows/rc0018-asc-mc.yml`

The gate binds RC-0018 to the production ASC/MC runtime, explicit UT1/TT and geographic inputs, central sidereal-time implementation, IAU 2006 mean obliquity, spherical geometry, normalization, pole/input fail-closed behavior, and compiled ASC/MC + sidereal regressions. The physical matrix now records RC-0018 as TESTED; it is intentionally blocked from VERIFIED/DONE pending independent authoritative golden vectors and end-to-end input propagation evidence.

## RC-0019 gate added; promotion pending

Added:

- `requirements/contracts/rc0019_house_cusps_contract.json`
- `tools/requirements/validate_rc0019_house_cusps.py`
- `.github/workflows/rc0019-house-cusps.yml`

The gate binds RC-0019 to the existing production Placidus/Porphyry house engines and requires twelve computed cusp longitudes, ASC/MC angular cusps, opposite-cusp invariants, iterative spherical Placidus geometry, ordered-cycle validation, fail-closed polar behavior by default, and explicit visible Porphyry fallback only when deliberately selected. It runs the compiled Placidus and Porphyry tests and may self-promote only to TESTED. At this checkpoint the physical matrix still says `RC-0019=NOT_STARTED`; do not report TESTED until the dedicated workflow succeeds and its bot matrix commit is physically visible.

## Next continuation

1. Re-read the latest `main` and exact RC-0019 workflow/matrix result. If red, inspect its failing job, repair the root cause, and rerun; if green, verify the physical `requirements(rc0019): record real house cusps TESTED` commit and matrix row.
2. Continue dependency order with RC-0020 and later independent requirements once RC-0019 has a proven machine state.
3. Keep open blockers explicit: RC-0003/0004 bilingual editorial provenance/review, RC-0005/0006/0007 AKİLES provenance, independent astronomy golden/tolerance gates including RC-1436/1437, RC-1439 physical UI references, signed reproducible clean-checkout release artifact, and real-device/offline/accessibility/PDF/Play lifecycle gates.

FINAL remains NO.
