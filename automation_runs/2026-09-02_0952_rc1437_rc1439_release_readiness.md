# RUH CODE automation checkpoint — RC-1437 / RC-1439 release readiness

Date: 2026-09-02

Binding scope remains `RC-0001 → RC-1442`. No requirement was promoted to DONE merely because validation code was added.

## RC-1437 — offline/versioned calculation data

Repository inspection confirmed that this requirement is not ready for release yet:

- `requirements/data_manifests/timezone.json` declares offline IANA timezone runtime and package `timezone` `0.10.1`.
- `requirements/data_manifests/cities.json` explicitly remains `SOURCE_SELECTED_NOT_BUNDLED`; generated catalog checksum is absent.
- `requirements/reference_manifests/offline_ephemeris_runtime.json` explicitly records planetary ephemeris and Earth-orientation data as not bundled/not proven, with missing SHA-256 evidence.
- `pubspec.yaml` does not declare physical city/ephemeris runtime assets.

Added `tools/requirements/validate_rc1437_offline_data.py`. Default mode is fail-closed and refuses release readiness unless physical/versioned/checksummed offline datasets are actually proven. `--allow-incomplete` exists only for audit/progress evidence and must not be used as a release pass.

Added `.github/workflows/rc1437-offline-data-readiness.yml` to continuously materialize JSON evidence without pretending the current known blockers are green.

Commits:

- `c56a36e7fe4bef209c9d9d7b7859c2592c6bdf7e` — fail-closed RC-1437 validator.
- `a619cc2ca81ec1f8b9c8adbc564ad1ae29b96957` — RC-1437 evidence workflow.

## RC-1439 — physical UI reference images

No canonical physical reference-image list with screen IDs + file names + SHA-256 evidence was found in the tracked requirement/reference manifests.

Added `requirements/reference_manifests/rc1439_reference_images.json` with explicit `NOT_PROVEN` status rather than inventing image identities/checksums. Placeholder-generated evidence is explicitly forbidden.

Added `tools/requirements/validate_rc1439_reference_images.py`; strict mode verifies:

- non-empty physical image entries,
- unique screen IDs and paths,
- repository-contained physical files,
- file-name/path agreement,
- SHA-256 format and exact checksum match,
- `BUNDLED_VERIFIED` status,
- fail-closed release behavior for missing/mismatched assets.

Added `.github/workflows/rc1439-reference-image-readiness.yml` to emit an audit artifact while the physical assets are still missing.

Commits:

- `52168528de7526e95c401ceff115891200607606` — explicit RC-1439 evidence manifest.
- `a24cb866cd490b4ad1f2fda5717cf5af8e36091d` — fail-closed RC-1439 validator.
- `68e4265a1bd23f76e9c7d13183391bc976b948ff` — RC-1439 evidence workflow.

## Release boundary

Neither audit workflow is a substitute for a strict release gate. Once physical artifacts are added, release/clean-checkout CI must invoke these validators without `--allow-incomplete`.

`requirements/requirement_state.csv` remains unchanged. RC-1437 and RC-1439 are not DONE.

Other known final blockers remain: canonical tracked/signable Android production host and application identity, signed reproducible clean-checkout artifact, real-device offline/airplane-mode evidence, independent astronomy golden evidence, production PDF font/license/device delivery evidence, and final exact 1,442-RC lifecycle audit.

FINAL: NO.
