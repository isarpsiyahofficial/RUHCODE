# RUH CODE — Checkpoint: June 2035 + CI root-cause repairs

## Binding scope

- Exact contract remains `RC-0001 → RC-1442`.
- No requirement is promoted to DONE without its mandatory evidence and release gates.
- FINAL remains forbidden while critical CI or release gates are red/unverified.

## Baseline re-read

Baseline HEAD inspected at the start of this run: `71829268300a5caa5b9e07a54255884f633098cf`.

GitHub Actions exposed 23 runs for that SHA. Four were completed failures:

1. Flutter Quality
2. Requirements Contract
3. Western Aspect Grid Contract
4. Western Natal Aspects Contract

This baseline is therefore explicitly NOT treated as CI SUCCESS.

## Daily-message progress

Added and committed:

- `assets/content/daily_messages/tr/2035-06.csv` — 30 canonical TR rows
- `assets/content/daily_messages/en/2035-06.csv` — 30 independent canonical EN rows

Both files were reloaded from `main` after commit and visibly verified for:

- canonical header `date,locale,title,teaser,full_text,theme_tag`
- correct locale per shard
- exact contiguous civil dates `2035-06-01 → 2035-06-30`

Editorial ledger advanced only after that re-read:

- TR: 3468
- EN: 3468
- total: 6936 / 8036
- remaining: 1100
- next exact start: `2035-07-01`

`RC-1424/1425/1426/1427/1433/1434` remain not-DONE until the full 8036-record strict release audit and exact CI/release evidence exist.

## CI root causes found and repaired

### Requirements Contract

Decoded job log showed the sequence and classification steps passing, then evidence integrity failing because `evidence/content/daily_messages_editorial_progress.json` listed directory paths (`assets/content/daily_messages/tr` and `/en`) under `sources`. The integrity contract requires file-resolvable repository paths.

Repair: removed directory entries and retained concrete file/tool paths only.

### Western Aspect Grid + Western Natal Aspects

Both decoded job logs failed from the same Dart runtime error:

`type 'int' is not a subtype of type 'double' in type cast`

Root cause was the default `Map<MajorAspect,double>` in `AspectOrbPolicy` being initialized with integer literals.

Repair: changed defaults to explicit doubles `8.0 / 5.0 / 7.0 / 7.0 / 8.0` without changing orb policy semantics.

### Flutter Quality

The baseline `flutter analyze --fatal-infos` reported 55 issues and skipped the test phase. This run removed verified invalid `const StateError` calls from:

- Google Play lifetime ownership synchronization
- rewarded temporary unlock
- combined professional PDF preview/build drift guards

The decoded baseline also identifies remaining backup test import drift, PDF/UI invalid const calls, old CivilDate test constructors, PDF symbol import drift, and fatal warning/info debt. Those are still open until a newer exact SHA proves the analyzer and tests green.

## Commits made in this checkpoint chain

- `dcce57d3cd87857c5871bfb97171cd69e03041bf` — June 2035 TR shard
- `d6aeae87737ccb4d2f5bed393bca58ed9257d797` — June 2035 EN shard
- `8dc5d994645ce8760acf9bf95368b63e0c2f4c43` — advance editorial ledger
- `d7e10276a2c7a790415071564cc9b3b4490f8b78` — explicit double orb defaults
- `ff7dd128f377d4476e4d84d661306cd401d248a2` — file-resolvable daily-message evidence paths
- `9a7b0b31e3520b291647f40dd7e6355f80716910` — ownership analyzer repair
- `ebc50ea1fa76d86f9ecc50c2de5bf2347bfdcbe1` — rewarded unlock analyzer repair
- `9a2987eb50c1c6351e41085d6d214786b10eb0cd` — combined PDF analyzer repairs

## Next safe work

1. Read completed workflow results for the newest exact HEAD and continue from decoded failures, not assumptions.
2. Finish Flutter analyzer/test cleanup, especially backup/PDF/UI symbol/import/constructor drift.
3. Continue independent TR/EN editorial coverage from `2035-07-01` while unrelated physical-device/artifact blockers remain open.
4. Never claim FINAL before all 1,442 RCs and release gates are verified.

**FINAL: NO.**
