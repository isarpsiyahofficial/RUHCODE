{
  "contract": "professional-pdf-local-renderer-v1",
  "status": "SOURCE_LEVEL_IMPLEMENTED",
  "done": false,
  "requirements": [
    "RC-0950",
    "RC-0951",
    "RC-0953"
  ],
  "localOnly": true,
  "dependency": {
    "package": "pdf",
    "constraint": "^3.13.0",
    "publisher": "nfet.net"
  },
  "requiredProperties": [
    "A4 dimensions and margins come from PdfReportPlan",
    "all selected sections belong to one exact SHA-256 calculation snapshot",
    "TR/EN production rendering requires bundled TrueType font bytes",
    "regular and bold font bytes are verified against declared SHA-256 digests",
    "font family and license identifiers are mandatory",
    "empty or unavailable sections cannot be rendered",
    "page numbers are generated locally",
    "renderer explicitly supports reports longer than 50 pages with a bounded 200-page safety ceiling",
    "section headings are protected against orphaning by a minimum remaining-space break and inseparable heading plus first paragraph",
    "generated bytes must pass PDF header final-EOF catalog pages-tree page-object and cross-reference structural inspection before return",
    "the Pages-tree declared Count is mandatory and must exactly match the actual Page-object count",
    "the final trailer must contain startxref immediately before the final EOF marker",
    "startxref must be an in-range byte offset that points to an xref table or XRef stream object",
    "the recognized xref table trailer or XRef stream dictionary must declare an indirect Root reference",
    "junk appended after the final EOF marker is rejected",
    "page-count verification can enforce exact or bounded page counts after structural validation",
    "5-page 25-page and 50-plus regression fixtures are covered at the structural page-count gate",
    "long tables are split into deterministic bounded chunks with repeated logical headers",
    "inconsistent table column widths are rejected before rendering"
  ],
  "remainingBeforeDone": [
    "bundle approved Unicode TR/EN fonts with license and immutable SHA-256 manifest",
    "run actual 5/25/50+ byte-render tests with approved font assets and verify page counts through the same gate",
    "run low-memory PDF generation tests",
    "add independent full-parser/open, missing-glyph, crop and visual-regression checks",
    "add Western/Vedic vector chart and BaZi/Numerology table render adapters",
    "obtain exact workflow SUCCESS evidence"
  ]
}
