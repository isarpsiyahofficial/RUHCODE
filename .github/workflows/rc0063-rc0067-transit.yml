name: RC-0063 RC-0067 Transit Core

on:
  push:
    branches: [main]
    paths:
      - 'RUH_CODE_MASTER_SARTNAME.md'
      - 'lib/src/calculation_core/western/transit_chart.dart'
      - 'lib/src/calculation_core/western/natal_chart.dart'
      - 'lib/src/calculation_core/western/natal_aspects.dart'
      - 'test/calculation_core/western/transit_chart_test.dart'
      - 'requirements/contracts/rc0063_rc0067_transit_contract.json'
      - 'tools/requirements/validate_rc0063_rc0067_transit.py'
      - '.github/workflows/rc0063-rc0067-transit.yml'
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: requirement-matrix-writers
  cancel-in-progress: false

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.7'
          channel: stable
          cache: true
      - run: flutter pub get
      - run: python tools/requirements/validate_rc0063_rc0067_transit.py
      - run: flutter test test/calculation_core/western/transit_chart_test.dart
      - name: Promote proven transit core to TESTED
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main
          git checkout -B main origin/main
          python tools/requirements/validate_rc0063_rc0067_transit.py
          python - <<'PY'
          import csv
          from pathlib import Path
          p=Path('requirements/requirement_state.csv')
          rows=list(csv.DictReader(p.open(encoding='utf-8',newline=''))); fields=list(rows[0].keys())
          links='requirements/contracts/rc0063_rc0067_transit_contract.json|tools/requirements/validate_rc0063_rc0067_transit.py|lib/src/calculation_core/western/transit_chart.dart|test/calculation_core/western/transit_chart_test.dart|.github/workflows/rc0063-rc0067-transit.yml'
          for r in rows:
            if r['rc_id'] not in {'RC-0063','RC-0064','RC-0065','RC-0066','RC-0067'}: continue
            if r['status'] not in ('VERIFIED','DONE'): r['status']='TESTED'
            r['evidence_links']=links
            r['blocked']='YES'
            r['blocker']='End-to-end date selection, rendered transit UI/timeline evidence, independent astronomy goldens and release/device gates remain required before VERIFIED/DONE.'
            r['notes']='Machine gate proves deterministic explicit-TT transit snapshots, historical/future instant support, natal×transit comparison and transit-to-natal aspect calculation with fail-closed provenance. TESTED only.'
          with p.open('w',encoding='utf-8',newline='') as f:
            w=csv.DictWriter(f,fieldnames=fields); w.writeheader(); w.writerows(rows)
          PY
          python tools/requirements/validate_requirement_matrix.py
          if git diff --quiet -- requirements/requirement_state.csv; then exit 0; fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add requirements/requirement_state.csv
          git commit -m 'requirements(rc0063-rc0067): record transit core TESTED'
          git push origin HEAD:main
